---
export interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<li>
	<div class="max-w-sm mx-auto bg-gray-400 shadow-lg rounded-lg overflow-hidden w-80 px-6 py-8">
		<h3 class="text-2xl font-semibold text-gray-700">
			{title}
		</he>
		<p class="mt-2 text-gray-600">
			{body}
		</p>
	</div>
</li>
